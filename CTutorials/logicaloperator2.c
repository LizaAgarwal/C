//print 0 or 1 for the statement if it's monday or it's raining then print 1
#include<stdio.h>

int main() {

    int isMonday=1;
    int isRaining=0;
    printf("%d",isMonday || isRaining);
    return 0;
}