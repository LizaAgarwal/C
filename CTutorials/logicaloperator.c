//print 1 or 0 for the statement if it's sunday & it's snowing then print 1
#include<stdio.h>

int main() {

    int isSunday= 0;
    int isSnowing= 1;
    printf("%d",isSunday && isSnowing);
    return 0;
}